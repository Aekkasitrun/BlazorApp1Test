@using BudgetBook.Data
@inject BudgetBookService budgetBookService

<input class="form-check-input" type="checkbox" @bind-value="CheckBoxTodo.IsChecked" id="flexCheckDefault" />

@code {
    [Parameter] public Todo CheckBoxTodo { get; set; }

    [Parameter] public EventCallback<Todo> UpdateCheckBoxTodo { get; set; }

    //private async Task CheckList()
    //{
    //    await budgetBookService.AddOrUpdateEntry(CheckBoxEntry);
    //    await UpdateCheckBoxEntry.InvokeAsync(CheckBoxEntry);

    //    // You can perform additional actions after updating the CheckBoxEntry if needed.
    //    Console.WriteLine("IsCheck = " + CheckBoxEntry.IsChecked);
    //}
}
